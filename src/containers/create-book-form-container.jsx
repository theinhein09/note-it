import React from "react";
import CreateBookForm from "../components/create-book-form";

const CreateBookFormContainer = (props) => {
  const createBook = () => {};
  return <CreateBookForm {...props} />;
};

export default CreateBookFormContainer;
